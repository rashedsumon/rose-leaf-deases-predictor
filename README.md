# Rose Leaf Disease Classifier Dashboard

An automated AI computer-vision workflow built using PyTorch and deployed via Streamlit Cloud to classify health metrics from rose leaf target inputs.

## Local Development Execution

1. Clone or download this project workspace repository folder structure.
2. Initialize environment parameters and execute configurations dependencies:
   ```bash
   pip install -r requirements.txt